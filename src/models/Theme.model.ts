type Theme = {
  border: string;
  text: {
    primary: string;
    secondary: string;
  };
  background: {
    primary: string;
    secondary: string;
  };
  inputBackground: {
    primary: string;
    secondary: string;
  }
  accent: {
    primary: string
  };
};

export default Theme;